[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
# Disable Docker layer cache to ensure client changes are always rebuilt
dockerNoCache = true

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
